/**
 * Unified dashboard route - consolidated SLAM and tracking workspace.
 *
 * Replaces page-to-page navigation with in-dashboard mode switching.
 */
'use client';

import { UnifiedDashboard } from '@/components/dashboard/unified-dashboard.tsx';

export default function DashboardPage() {
    return <UnifiedDashboard />;
}
