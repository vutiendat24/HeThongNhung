## UC-02: Tạo lập bản đồ bằng di chuyển tự động

### Mô tả use case

| Mục                            | Nội dung                                                                                                                                                                                                                     |
| ------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Phụ thuộc                      | UC-01 (chia sẻ cơ sở hạ tầng SLAM Mapping)                                                                                                                                                                                   |
| Mục đích                       | Người vận hành cần quét bản đồ toàn bộ không gian mà không phải điều khiển thủ công. PM cho phép robot tự động khám phá các vùng chưa biết (frontier exploration) để xây dựng bản đồ hoàn chỉnh mà không cần can thiệp liên tục. |
| Mô tả                          | Người vận hành bật chế độ Auto Exploration, hệ thống sử dụng thuật toán frontier exploration (m-explore) để tự động điều hướng robot đến các biên giới chưa khám phá, kết hợp SLAM Toolbox xây dựng bản đồ cho đến khi toàn bộ không gian được quét. |
| Actor chính                    | Người vận hành (Operator)                                                                                                                                                                                                    |
| Actor liên quan                | SLAM Toolbox (xử lý SLAM), explore_lite (frontier exploration), Nav2 planner (lập đường đi), ESP32 firmware (điều khiển robot)                                                                                                |
| Tiền điều kiện                 | 1. Robot đã bật nguồn và kết nối WiFi <br> 2. Web dashboard đã kết nối rosbridge (status = connected) <br> 3. Hệ thống đang ở chế độ SLAM Mapping <br> 4. explore_lite node đã được launch (use_explore = true)                |
| Dãy lệnh thực hiện bình thường | 1. Người vận hành mở web dashboard và xác nhận kết nối ROS thành công <br> 2. Người vận hành chọn chế độ "Mapping" trên Mode Controller <br> 3. Người vận hành bật toggle "Auto Exploration" <br> 4. Hệ thống publish /explore/resume = true → explore_lite bắt đầu tìm frontier <br> 5. explore_lite xác định frontier gần nhất và gửi navigation goal <br> 6. Robot tự động di chuyển đến frontier, SLAM Toolbox cập nhật bản đồ <br> 7. Quá trình lặp lại cho đến khi không còn frontier (exploration complete) <br> 8. Người vận hành nhấn "Save Map" để lưu bản đồ hoàn chỉnh |
| Hậu điều kiện (thành công)     | Bản đồ 2D hoàn chỉnh (.yaml + .pgm) được lưu, phủ toàn bộ không gian có thể tiếp cận                                                                                                                                        |
| Hậu điều kiện (thất bại)       | Robot dừng tại vị trí hiện tại, bản đồ một phần vẫn còn trong bộ nhớ SLAM (có thể lưu phần đã quét hoặc chuyển sang điều khiển thủ công)                                                                                      |
| Xử lý ngoại lệ                 | Robot bị kẹt (progress_timeout) → explore_lite thử frontier khác <br> Không còn frontier nhưng bản đồ chưa đầy đủ → Người vận hành điều khiển thủ công bổ sung <br> Mất kết nối → Robot dừng (safety timeout), exploration tạm dừng |

### Lược đồ tuần tự

```plantuml
@startuml UC-02
title UC-02: Tạo lập bản đồ bằng di chuyển tự động

actor "Người vận hành" as Operator
participant "Hệ thống" as System
participant "explore_lite" as Explore
participant "SLAM Toolbox" as SLAM

Operator -> System: toggleAutoExplore(enabled=true)
System -> Explore: /explore/resume(data=true)
Explore -> Explore: Phân tích bản đồ, tìm frontier

loop Còn frontier chưa khám phá
    Explore -> System: NavigateToPose(frontier_pose)
    System -> System: Robot di chuyển đến frontier
    System -> SLAM: /scan + /odom
    SLAM -> SLAM: Cập nhật bản đồ
    SLAM --> System: /map (OccupancyGrid)
    System --> Operator: Hiển thị bản đồ + trạng thái exploration
    Explore -> Explore: Tìm frontier tiếp theo
end

Explore --> System: ExploreStatus(status=complete)
System --> Operator: "Exploration complete"

Operator -> System: saveMap(map_name)
alt Lưu thất bại
    System --> Operator: Error + "Failed to save map"
else Lưu thành công
    System -> SLAM: serialize_map(map_name)
    SLAM --> System: success
    System --> Operator: Success + "Map saved"
end
@enduml
```

### Lược đồ hoạt động

```plantuml
@startuml UC-02-activity
title UC-02: Tạo lập bản đồ bằng di chuyển tự động - Activity Diagram

start

:Người vận hành kết nối dashboard;

if (Rosbridge connected?) then (không)
  :Hiển thị lỗi kết nối;
  stop
else (có)
endif

if (explore_lite node available?) then (không)
  :Hiển thị cảnh báo: explore không khả dụng;
  stop
else (có)
endif

:Chọn chế độ Mapping;
:Bật Auto Exploration;
:Publish /explore/resume = true;

repeat
  :explore_lite phân tích bản đồ hiện tại;

  if (Còn frontier?) then (không)
    :Exploration hoàn tất;
    break
  else (có)
  endif

  :Chọn frontier tối ưu;
  :Gửi NavigateToPose đến frontier;
  :Robot di chuyển, SLAM cập nhật bản đồ;

  if (Robot bị kẹt (timeout)?) then (có)
    :Chọn frontier khác;
  else (không)
  endif

repeat while (Auto Explore vẫn bật?) is (có)
-> không;

:Người vận hành nhấn Save Map;

if (Lưu thành công?) then (có)
  :Bản đồ hoàn chỉnh được lưu;
else (không)
  :Hiển thị lỗi, cho phép thử lại;
endif

stop
@enduml
```

### Lược đồ trạng thái

```plantuml
@startuml UC-02-state
title UC-02: Tạo lập bản đồ bằng di chuyển tự động - State Diagram

[*] --> Idle
Idle --> Exploring: Operator bật Auto Explore
Exploring --> NavigatingToFrontier: Frontier được xác định
NavigatingToFrontier --> Exploring: Đến frontier, quét xong
NavigatingToFrontier --> RecoveringFromStuck: Progress timeout
RecoveringFromStuck --> Exploring: Recovery thành công
RecoveringFromStuck --> Paused: Recovery thất bại
Exploring --> ExplorationComplete: Không còn frontier
Exploring --> Paused: Operator tắt Auto Explore
Paused --> Exploring: Operator bật lại Auto Explore
Paused --> Idle: Operator chuyển mode
ExplorationComplete --> SavingMap: Operator nhấn Save
SavingMap --> MapSaved: Lưu thành công
SavingMap --> ExplorationComplete: Lưu thất bại
MapSaved --> [*]
@enduml
```

### Lược đồ lớp ý niệm

```plantuml
@startuml UC-02-class
title UC-02: Tạo lập bản đồ bằng di chuyển tự động - Conceptual Class Diagram

class "ExploreState" as ExploreState {
  - isExecuting: boolean
  - feedback: null
  - error: string | null
  + sendGoal(): void
  + cancel(): void
}

class "ExploreStatus" as ExploreStatus {
  - status: string
}

class "OccupancyGrid" as OccupancyGrid {
  - header: Header
  - info: MapMetaData
  - data: int8[]
}

class "PoseStamped" as PoseStamped {
  - header: Header
  - pose: Pose
}

class "Pose" as Pose {
  - position: Point
  - orientation: Quaternion
}

class "FrontierPoint" as Frontier {
  - position: Point
  - size: float
  - cost: float
}

ExploreState --> ExploreStatus: monitors
ExploreState --> PoseStamped: generates goal
PoseStamped *-- Pose
OccupancyGrid --> Frontier: identifies frontiers
@enduml
```

### Phân rã thành phần PM

#### Controller: `DashboardWebApp`

- **Nhiệm vụ**: Nhận lệnh bật/tắt Auto Exploration từ người vận hành, publish
  tín hiệu resume/pause qua rosbridge.
- **Topic publish**: `/explore/resume` (std_msgs/Bool)
- **Topic subscribe**: `/explore/status` (explore_lite_msgs/ExploreStatus)
- **Input**: Toggle switch event → `Bool { data: true/false }`
- **Output thành công**: Hiển thị trạng thái exploration (exploring/paused/complete)
- **Output lỗi**: Toast notification lỗi kết nối

#### UseCase: `AutoExplorationUseCase`

- **Nhiệm vụ**: Orchestrate luồng frontier exploration tự động.
- **Input**: `Bool` — `{ data: true }` (bật exploration)
- **Output**: `ExploreStatus` (trạng thái exploration real-time)
- **Gọi đến**:
    - `rosbridge.publish(/explore/resume)` — bật/tắt exploration
    - `rosbridge.subscribe(/explore/status)` — theo dõi trạng thái
    - `rosbridge.subscribe(/map)` — nhận bản đồ cập nhật

#### Node: `explore_lite`

- **Nhiệm vụ**: Thuật toán frontier exploration — phân tích bản đồ, xác định
  biên giới chưa khám phá, gửi navigation goal.
- **Subscribe**: `/map` (costmap), `/explore/resume` (điều khiển)
- **Publish**: `/explore/status` (ExploreStatus)
- **Action client**: `NavigateToPose` — gửi goal đến Nav2 hoặc move_base
- **Parameters**:
    - `planner_frequency`: 0.33 Hz
    - `progress_timeout`: 45.0s
    - `min_frontier_size`: 0.35m

#### Port: `SLAM Toolbox`

- **Nhiệm vụ**: Xử lý thuật toán SLAM, xây dựng bản đồ từ /scan + /odom.
- **Subscribe**: `/scan`, TF (odom → base_footprint)
- **Publish**: `/map` (nav_msgs/OccupancyGrid)
- **Service**: `/slam_toolbox/serialize_map` — lưu bản đồ ra file

#### Lược đồ tuần tự nội bộ PM

```plantuml
@startuml UC-02-internal
title UC-02: Tạo lập bản đồ tự động - Internal Sequence

actor "Người vận hành" as Operator
participant "Web Dashboard" as Web
participant "rosbridge" as ROS
participant "explore_lite" as Explore
participant "Nav2 Planner" as Planner
participant "ESP32" as MCU
participant "SLAM Toolbox" as SLAM
database "File System" as FS

== Bật Auto Exploration ==
Operator -> Web: Toggle Auto Explore ON
Web -> ROS: publish /explore/resume {data: true}
ROS -> Explore: /explore/resume

== Vòng lặp Exploration ==
loop Còn frontier
    Explore -> Explore: Phân tích /map, tìm frontier
    Explore -> Planner: NavigateToPose(frontier_pose)
    Planner -> MCU: /cmd_vel (planned path)
    MCU -> MCU: PID motor control
    MCU -> ROS: publish /scan + /odom
    ROS -> SLAM: /scan + TF
    SLAM -> SLAM: SLAM processing
    SLAM -> ROS: publish /map
    ROS -> Explore: /map (updated)
    ROS -> Web: /map
    Web --> Operator: Render bản đồ + status
end

Explore -> ROS: publish /explore/status {status: complete}
ROS -> Web: /explore/status
Web --> Operator: "Exploration complete"

== Lưu bản đồ ==
Operator -> Web: Click "Save Map" (map_name)
Web -> ROS: call /slam_toolbox/serialize_map(map_name)
ROS -> SLAM: serialize_map(map_name)
SLAM -> FS: Write .yaml + .pgm
FS --> SLAM: OK
SLAM --> ROS: result = 0
ROS --> Web: SaveMapResponse(result=0)
Web --> Operator: "Map saved successfully"
@enduml
```

#### Giao diện

##### Giao diện mẫu

```plantuml
@startsalt
{+
  <b>SLAM Mapping - Auto Exploration
  ..
  {
    [<b>Mapping</b>] | [Navigation]
  }
  ==
  {+
    {* Occupancy Grid Map (real-time) + Frontier markers }
  }
  ..
  {
    { Auto Exploration  | [X] ON  }
    { Status: | Exploring frontiers... }
  }
  ..
  {
    { Map name: | "full_room" }
    { [Save Map] }
  }
}
@endsalt
```

##### Giao diện ứng dụng

Chưa hiện thực. Sẽ bổ sung ảnh chụp màn hình khi hoàn thành.
