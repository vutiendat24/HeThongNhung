## UC-03: Di chuyển đến một điểm bất kỳ trên bản đồ đã quét

### Mô tả use case

| Mục                            | Nội dung                                                                                                                                                                                                              |
| ------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Phụ thuộc                      | UC-01 hoặc UC-02 (cần có bản đồ đã lưu trước đó)                                                                                                                                                                     |
| Mục đích                       | Người vận hành cần robot di chuyển đến một vị trí cụ thể trong không gian đã được quét. PM cho phép người vận hành click vào bản đồ để chọn điểm đích, hệ thống tự động lập đường đi và điều hướng robot đến đó an toàn. |
| Mô tả                          | Người vận hành chuyển sang chế độ Navigation, load bản đồ đã lưu, đặt vị trí ban đầu (initial pose), sau đó click vào bản đồ để đặt điểm đích. Hệ thống sử dụng Nav2 stack để lập đường đi và điều hướng robot tránh vật cản. |
| Actor chính                    | Người vận hành (Operator)                                                                                                                                                                                             |
| Actor liên quan                | Nav2 Stack (planner + controller + BT navigator), AMCL (định vị), Map Server (cung cấp bản đồ), ESP32 firmware (điều khiển robot)                                                                                     |
| Tiền điều kiện                 | 1. Robot đã bật nguồn và kết nối WiFi <br> 2. Web dashboard đã kết nối rosbridge (status = connected) <br> 3. Có ít nhất một bản đồ đã lưu trong thư mục maps <br> 4. Hệ thống đang ở chế độ Navigation (Nav2 stack active) |
| Dãy lệnh thực hiện bình thường | 1. Người vận hành chuyển sang chế độ "Navigation" trên Mode Controller <br> 2. Hệ thống gọi /map_manager/set_mode(NAVIGATION) → activate Nav2 stack <br> 3. Người vận hành chọn bản đồ từ danh sách và nhấn Load <br> 4. Hệ thống gọi /map_manager/load_map → Map Server load bản đồ <br> 5. Người vận hành đặt Initial Pose (vị trí hiện tại của robot trên bản đồ) <br> 6. AMCL bắt đầu định vị robot trên bản đồ <br> 7. Người vận hành click vào điểm đích trên bản đồ <br> 8. Hệ thống gửi NavigateToPose action goal đến Nav2 <br> 9. Nav2 lập đường đi (global planner) và điều hướng robot (local controller) <br> 10. Robot di chuyển đến đích, tránh vật cản động <br> 11. Nav2 trả kết quả thành công khi robot đến đích |
| Hậu điều kiện (thành công)     | Robot đã đến vị trí đích trên bản đồ, sẵn sàng nhận goal mới                                                                                                                                                          |
| Hậu điều kiện (thất bại)       | Robot dừng tại vị trí hiện tại, navigation bị cancel hoặc abort. Người vận hành có thể thử lại với điểm đích khác hoặc điều khiển thủ công.                                                                            |
| Xử lý ngoại lệ                 | Đường đi bị chặn hoàn toàn → Nav2 trả lỗi, UI hiển thị thông báo <br> Robot bị kẹt (recovery behavior) → Nav2 thử spin/backup recovery <br> Người vận hành cancel → Gửi cancel goal, robot dừng <br> Mất kết nối → Robot dừng (safety timeout) |

### Lược đồ tuần tự

```plantuml
@startuml UC-03
title UC-03: Di chuyển đến một điểm bất kỳ trên bản đồ đã quét

actor "Người vận hành" as Operator
participant "Hệ thống" as System
participant "Nav2 Stack" as Nav2

== Chuẩn bị ==
Operator -> System: setMode(NAVIGATION)
System -> Nav2: Activate Nav2 lifecycle nodes
Nav2 --> System: Active

Operator -> System: loadMap(map_name)
System -> Nav2: /map_server/load_map(map_name)
Nav2 --> System: Map loaded

Operator -> System: setInitialPose(pose)
System -> Nav2: /initialpose (PoseWithCovarianceStamped)
Nav2 --> System: AMCL localized

== Điều hướng ==
Operator -> System: clickOnMap(x, y) → NavigateToPose(goal_pose)
alt Không tìm được đường đi
    System -> Nav2: NavigateToPose(goal_pose)
    Nav2 --> System: ABORTED + "No valid path"
    System --> Operator: Error "Cannot reach destination"
else Người vận hành cancel
    Operator -> System: cancelNavigation()
    System -> Nav2: cancel_goal
    Nav2 --> System: CANCELED
    System --> Operator: "Navigation cancelled"
else Điều hướng thành công
    System -> Nav2: NavigateToPose(goal_pose)
    loop Di chuyển đến đích
        Nav2 -> System: feedback(current_pose, distance_remaining, ETA)
        System --> Operator: Hiển thị tiến trình + đường đi
    end
    Nav2 --> System: SUCCEEDED
    System --> Operator: "Arrived at destination"
end
@enduml
```

### Lược đồ hoạt động

```plantuml
@startuml UC-03-activity
title UC-03: Di chuyển đến điểm đích - Activity Diagram

start

:Người vận hành chuyển sang chế độ Navigation;

if (Set mode thành công?) then (không)
  :Hiển thị lỗi chuyển mode;
  stop
else (có)
endif

:Chọn bản đồ từ danh sách;
:Load bản đồ;

if (Load map thành công?) then (không)
  :Hiển thị lỗi load map;
  stop
else (có)
endif

:Đặt Initial Pose;
:AMCL định vị robot trên bản đồ;

:Người vận hành click điểm đích trên bản đồ;
:Chuyển tọa độ pixel → tọa độ map (x, y);
:Gửi NavigateToPose goal;

if (Nav2 tìm được đường đi?) then (không)
  :Trả lỗi "No valid path";
  :Cho phép chọn điểm đích khác;
  stop
else (có)
endif

repeat
  :Nav2 điều hướng robot theo path;
  :Gửi feedback (distance_remaining, ETA);
  :Hiển thị tiến trình trên UI;

  if (Gặp vật cản động?) then (có)
    :Local controller tránh vật cản;
  else (không)
  endif

  if (Robot bị kẹt?) then (có)
    :Thực hiện recovery behavior (spin/backup);
    if (Recovery thành công?) then (không)
      :Navigation ABORTED;
      stop
    else (có)
    endif
  else (không)
  endif

repeat while (Đã đến đích?) is (chưa)
-> rồi;

:Navigation SUCCEEDED;
:Hiển thị "Arrived at destination";

stop
@enduml
```

### Lược đồ trạng thái

```plantuml
@startuml UC-03-state
title UC-03: Di chuyển đến điểm đích - State Diagram

[*] --> Idle: Mode = Navigation, Map loaded

Idle --> GoalReceived: Operator click điểm đích
GoalReceived --> Planning: Nav2 nhận goal
Planning --> Navigating: Path found
Planning --> Failed: No valid path
Navigating --> Navigating: Feedback update
Navigating --> Recovering: Robot stuck
Recovering --> Navigating: Recovery thành công
Recovering --> Failed: Recovery thất bại
Navigating --> Succeeded: Đến đích
Navigating --> Cancelled: Operator cancel
GoalReceived --> Cancelled: Operator cancel

Succeeded --> Idle: Sẵn sàng nhận goal mới
Failed --> Idle: Sẵn sàng nhận goal mới
Cancelled --> Idle: Sẵn sàng nhận goal mới
@enduml
```

### Lược đồ lớp ý niệm

```plantuml
@startuml UC-03-class
title UC-03: Di chuyển đến điểm đích - Conceptual Class Diagram

class "NavState" as NavState {
  - isExecuting: boolean
  - feedback: NavigateToPoseFeedback
  - result: NavigateToPoseResult
  - error: string | null
  - currentGoal: PoseStamped
  + sendGoal(pose): void
  + cancel(): void
}

class "PoseStamped" as PoseStamped {
  - header: Header
  - pose: Pose
}

class "Pose" as Pose {
  - position: Point
  - orientation: Quaternion
}

class "Point" as Point {
  - x: float
  - y: float
  - z: float
}

class "Quaternion" as Quaternion {
  - x: float
  - y: float
  - z: float
  - w: float
}

class "NavigateToPoseFeedback" as Feedback {
  - current_pose: PoseStamped
  - navigation_time: Duration
  - estimated_time_remaining: Duration
  - number_of_recoveries: int
  - distance_remaining: float
}

class "OccupancyGrid" as OccupancyGrid {
  - header: Header
  - info: MapMetaData
  - data: int8[]
}

NavState --> PoseStamped: currentGoal
NavState --> Feedback: receives
PoseStamped *-- Pose
Pose *-- Point
Pose *-- Quaternion
@enduml
```

### Phân rã thành phần PM

#### Controller: `DashboardWebApp`

- **Nhiệm vụ**: Nhận click trên bản đồ từ người vận hành, chuyển đổi tọa độ
  pixel sang tọa độ map, gửi navigation goal qua rosbridge.
- **Service call**: `/map_manager/set_mode`, `/map_manager/load_map`
- **Topic publish**: `/initialpose` (geometry_msgs/PoseWithCovarianceStamped)
- **Action client**: `/navigate_to_pose` (nav2_msgs/NavigateToPose)
- **Input**: Click event (clientX, clientY) → `PoseStamped { header.frame_id: "map", pose.position: {x, y} }`
- **Output thành công**: Hiển thị tiến trình navigation (distance, ETA), thông báo đến đích
- **Output lỗi**: Toast notification lỗi (no path, aborted, cancelled)

#### UseCase: `NavigateToGoalUseCase`

- **Nhiệm vụ**: Orchestrate luồng điều hướng đến điểm đích.
- **Input**: `PoseStamped` — `{ frame_id: "map", position: {x, y, z}, orientation: {x, y, z, w} }`
- **Output**: `NavigateToPoseResult` + `NavigateToPoseFeedback` (real-time)
- **Gọi đến**:
    - `rosbridge.sendGoal(/navigate_to_pose)` — gửi goal đến Nav2
    - `rosbridge.cancelGoal()` — hủy navigation
    - `rosbridge.callService(/map_manager/set_mode)` — chuyển mode
    - `rosbridge.callService(/map_manager/load_map)` — load bản đồ

#### Node: `Nav2 Stack`

- **Nhiệm vụ**: Lập đường đi và điều hướng robot đến đích an toàn.
- **Thành phần**:
    - `map_server` — cung cấp bản đồ static
    - `amcl` — định vị robot trên bản đồ (Adaptive Monte Carlo Localization)
    - `planner_server` — lập đường đi toàn cục (NavFn/Dijkstra)
    - `controller_server` — điều khiển cục bộ (DWB)
    - `bt_navigator` — Behavior Tree orchestrator
    - `behavior_server` — recovery behaviors (spin, backup)
- **Action server**: `/navigate_to_pose` (nav2_msgs/NavigateToPose)
- **Publish**: `/cmd_vel` (geometry_msgs/Twist) → ESP32

#### Node: `MapManagerNode`

- **Nhiệm vụ**: Quản lý chuyển đổi mode và load bản đồ.
- **Service servers**:
    - `/map_manager/set_mode` — chuyển giữa SLAM_MAPPING và NAVIGATION
    - `/map_manager/load_map` — load bản đồ từ file
    - `/map_manager/list_maps` — liệt kê bản đồ có sẵn
- **Lifecycle management**: Activate/deactivate Nav2 nodes

#### Lược đồ tuần tự nội bộ PM

```plantuml
@startuml UC-03-internal
title UC-03: Di chuyển đến điểm đích - Internal Sequence

actor "Người vận hành" as Operator
participant "Web Dashboard" as Web
participant "rosbridge" as ROS
participant "MapManager" as MM
participant "Map Server" as MapSrv
participant "AMCL" as AMCL
participant "BT Navigator" as BT
participant "Planner" as Planner
participant "Controller" as Controller
participant "ESP32" as MCU

== Chuyển mode + Load map ==
Operator -> Web: Chọn Navigation mode
Web -> ROS: call /map_manager/set_mode(NAVIGATION)
ROS -> MM: set_mode(NAVIGATION)
MM -> MM: Activate Nav2 lifecycle nodes
MM --> ROS: success
ROS --> Web: SetModeResponse(success=true)

Operator -> Web: Chọn bản đồ, nhấn Load
Web -> ROS: call /map_manager/load_map(map_name)
ROS -> MM: load_map(map_name)
MM -> MapSrv: /map_server/load_map(path)
MapSrv --> MM: success
MM --> ROS: success
ROS --> Web: LoadMapResponse(success=true)

== Đặt Initial Pose ==
Operator -> Web: Đặt initial pose
Web -> ROS: publish /initialpose (PoseWithCovarianceStamped)
ROS -> AMCL: /initialpose
AMCL -> AMCL: Particle filter initialization

== Navigation ==
Operator -> Web: Click điểm đích trên bản đồ
Web -> Web: Pixel → Map coordinates
Web -> ROS: sendGoal /navigate_to_pose (PoseStamped)
ROS -> BT: NavigateToPose goal

BT -> Planner: compute_path(start, goal)
Planner -> Planner: Global path planning
Planner --> BT: Path

loop Đến đích
    BT -> Controller: follow_path(path)
    Controller -> ROS: publish /cmd_vel
    ROS -> MCU: /cmd_vel
    MCU -> MCU: PID motor control
    MCU -> ROS: publish /scan + /odom
    ROS -> AMCL: /scan (localization update)
    BT -> ROS: feedback(current_pose, distance_remaining)
    ROS -> Web: NavigateToPose feedback
    Web --> Operator: Hiển thị tiến trình
end

BT --> ROS: NavigateToPose result (SUCCEEDED)
ROS --> Web: result
Web --> Operator: "Arrived at destination"
@enduml
```

#### Giao diện

##### Giao diện mẫu

```plantuml
@startsalt
{+
  <b>Navigation Mode
  ..
  {
    [Mapping] | [<b>Navigation</b>]
  }
  ==
  {+
    {* Bản đồ + Robot position + Goal marker + Path }
    { <i>Click anywhere to set destination }
  }
  ..
  {
    { Navigation Status }
    { Distance: | 2.3m remaining }
    { ETA:      | ~8s            }
    { [Cancel Navigation]        }
  }
  ..
  {
    { Load Map: | ^my_room^ }
    { [Load] | [Refresh] }
  }
}
@endsalt
```

##### Giao diện ứng dụng

Chưa hiện thực. Sẽ bổ sung ảnh chụp màn hình khi hoàn thành.
